import profiles from '../data/profiles.json';

// const initialProfiles = JSON.parse(profiles);

const profilesListReducer = (state = [], action) => {
  switch (action) {
    default:
      return state
  }
}

export default profilesListReducer;